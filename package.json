{
  "name": "signalboard-pro",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "npm run -w api dev & npm run -w web dev",
    "build": "npm run -w api build && npm run -w web build",
    "prisma:generate": "npm run -w api prisma:generate",
    "prisma:migrate": "npm run -w api prisma:migrate",
    "seed": "npm run -w api seed"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  }
}
